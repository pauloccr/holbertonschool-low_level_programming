#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a: first integer
 * @b: second integer
 * Return: result of adding integers
 */
int add(int a, int b)
{
	return (a + b);
}
