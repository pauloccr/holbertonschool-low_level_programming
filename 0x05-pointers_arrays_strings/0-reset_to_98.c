#include <stdio.h>
#include "main.h"

/**
 * main - takes a pointer to an int as parameter and updates the value
 * it points to 98
 * Return: 0
 */

int main(void);

{
	int n = 402;
	int *p = &n;x
	printf("number is %p\n", p);
	*p = 98;
	printf("number is %p\n", p);
	return (0);
}
